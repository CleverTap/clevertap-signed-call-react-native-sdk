import { SC_ACCOUNT_ID, SC_API_KEY } from '@env';

export const Constants = {
  SC_ACCOUNT_ID,
  SC_API_KEY,
  KEY_LOGGED_IN_CUID: 'loggedInCuid',
  KEY_SEGMENT: 'segment',
  KEY_SC_DETAILS:'scDetails'
};
